package com.mycompany.app.week2.interfaces;

public class Ex6 {
    /* In Java, all methods in an interface are implicitly public, even if the public
    keyword is not used. This is because the purpose of an interface is to specify a
    contract that classes implementing the interface must follow, and that contract must
    be accessible to any class that wants to implement it.*/
}
